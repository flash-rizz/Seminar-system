# Class Diagram (PlantUML)

`class-diagram.puml` is aligned to the current `.java` classes in this repo.

Notes:
- `Report` and `Award` are included as `<<TODO>>` placeholders to satisfy the lab wording, but they are **not implemented** in code yet.

